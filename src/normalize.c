#include "./push_swap.h"

void normalize(t_stack *stack)
{
	x = 0;
	while x < argc 
		i = 0	
		lower = 2147483647;
		while argv[i]
			if x < lowest && !arr[i]
				lowest_pos = i;
			i++
		arr[lowest_pos] = 1;
		x++
}

 